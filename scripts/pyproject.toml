[project]
name = "scripts"
version = "0.1.0"
description = "Scripts for the blog"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "google-generativeai>=0.8.6",
    "pygithub>=2.8.1",
]
